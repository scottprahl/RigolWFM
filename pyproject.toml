[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
ignore = ["F403"]
exclude =['.git', '__pycache__', 'docs', 'wfm']

[tool.check-manifest]
ignore = [
    ".readthedocs.yaml",
    "todo",
    "docs/*",
    "Makefile",
    "release.txt",
    "requirements-dev.txt",
    "wfm/*",
    "ksy/*"
]
